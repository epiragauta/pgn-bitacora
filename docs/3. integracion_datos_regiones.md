# Plan de integración — Sección 3: Regionalización de la Inversión

**Fuente:** `BASES_BITACORA/2026/Marzo/3. REGIONALIZACIÓN/Consolidado Reg-Ejec-Marzo-2022-2026.xlsx`
**Hoja:** `Regionalizacion Mar-2022-2026`
**Fecha análisis:** 2026-06-28

---

## 1. Análisis del archivo fuente

### 1.1 Estructura general

| Elemento | Detalle |
|---|---|
| Años cubiertos | 2022, 2023, 2024, 2025, 2026 |
| Columnas por año | Apropiación · Compromisos · Obligaciones · Pagos |
| Unidad declarada | Millones de pesos corrientes |
| Unidad en DB (otras secciones) | Miles de millones (mmm) → dividir por 1,000 |
| Total filas de datos | 44 (sin encabezados ni totales) |

### 1.2 Jerarquía de datos

```
├── REGIÓN (fila en MAYÚSCULAS) → total de la región
│   └── Departamento (Título) → dato individual
├── POR REGIONALIZAR → inversión sin asignación territorial
├── NACIONAL → programas de alcance nacional
└── Total general → gran total
```

**Regiones y departamentos identificados:**

| Región | Departamentos |
|---|---|
| AMAZONAS | Amazonas, Caquetá, Guainía, Guaviare, Putumayo, Vaupés |
| ANDINA | Antioquia, Bogotá D.C., Boyacá, Caldas, Cundinamarca, Huila, Norte de Santander, Quindío, Risaralda, Santander, Tolima |
| CARIBE | Atlántico, Bolívar, Cesar, Córdoba, La Guajira, Magdalena, Sucre |
| INSULAR | Archipiélago de San Andrés, Providencia y Santa Catalina |
| ORINOQUÍA | Arauca, Casanare, Meta, Vichada |
| PACÍFICO | Cauca, Chocó, Nariño, Valle del Cauca |

> **Nota:** Filas CAPS y su equivalente en Título Case (ej. `POR REGIONALIZAR` / `Por Regionalizar`) contienen los mismos valores — son redundantes. La fila CAPS se descarta en la carga (se recalcula como suma).

### 1.3 Muestra de datos (2026, apropiación en mmm)

| Región | Aprop. | Comp. | Oblig. | Pagos |
|---|---|---|---|---|
| AMAZONAS | 13.14 | 5.70 | 1.71 | 1.70 |
| ANDINA | 133.44 | 65.96 | 19.24 | 19.19 |
| CARIBE | 71.98 | 34.65 | 10.00 | 9.72 |
| INSULAR | 2.00 | 0.51 | 0.14 | 0.14 |
| ORINOQUÍA | 12.53 | 6.61 | 1.78 | 1.76 |
| PACÍFICO | 49.94 | 22.71 | 6.95 | 6.63 |
| Por Regionalizar | 11.21 | 0.66 | 0.11 | 0.11 |
| Nacional | 124.07 | 52.15 | 9.49 | 9.20 |
| **Total general** | **418.31** | **188.95** | **49.41** | **48.44** |

---

## 2. Diagnóstico del esquema actual

### Problemas identificados

| Tabla actual | Problema |
|---|---|
| `regionalizacion_resumen` | **Sin columna `vigencia`** → no puede almacenar datos multi-año |
| `regionalizacion_detalle_2025` | Tabla nombrada para un año específico → no escalable; faltan columnas `vigencia` y `codigo_dane` |
| Ambas tablas | Sin códigos DANE estándar → no hay join con capas GeoJSON |
| Ambas tablas | Sin columna `tipo` → no distingue región/dpto/nacional/por_regionalizar |
| `_mm` en nombre de columnas | Inconsistente con el resto del esquema (que usa `_mmm`) |

### Lo que sí aplica del esquema actual

- La estructura conceptual (región → departamento) es correcta.
- Los campos `apropiacion`, `compromisos`, `obligaciones`, `pagos` son los correctos.
- Los porcentajes de ejecución (`pct_compromiso`, `pct_obligacion`) son útiles.

---

## 3. Esquema propuesto

### 3.1 Tabla catálogo de departamentos (nueva)

```sql
CREATE TABLE IF NOT EXISTS dane_departamentos (
    codigo     TEXT PRIMARY KEY,    -- '05', '11', '91', etc.
    nombre     TEXT NOT NULL,       -- nombre normalizado
    region     TEXT NOT NULL        -- región a la que pertenece
);
```

### 3.2 Tabla de regionalización unificada (reemplaza las dos actuales)

```sql
CREATE TABLE IF NOT EXISTS regionalizacion (
    id               INTEGER PRIMARY KEY AUTOINCREMENT,
    bitacora_id      INTEGER REFERENCES metadatos_bitacora(id),
    vigencia         INTEGER NOT NULL,
    tipo             TEXT    NOT NULL,  -- 'departamento' | 'por_regionalizar' | 'nacional'
    region           TEXT    NOT NULL,  -- ej. 'AMAZONIA', 'ANDINA'...
    departamento     TEXT    NOT NULL,  -- nombre del dpto o 'Por Regionalizar' / 'Nacional'
    codigo_dane      TEXT    REFERENCES dane_departamentos(codigo), -- NULL para especiales
    apropiacion_mmm  DECIMAL(14,6),    -- miles de millones pesos corrientes
    compromisos_mmm  DECIMAL(14,6),
    obligaciones_mmm DECIMAL(14,6),
    pagos_mmm        DECIMAL(14,6),
    pct_compromisos  DECIMAL(5,2),     -- % comp/aprop
    pct_obligaciones DECIMAL(5,2),
    pct_pagos        DECIMAL(5,2),
    pct_participacion DECIMAL(5,2),    -- % del total general del año
    UNIQUE(bitacora_id, vigencia, region, departamento)
);

CREATE INDEX IF NOT EXISTS idx_regionalizacion_vigencia
    ON regionalizacion(vigencia, region);
CREATE INDEX IF NOT EXISTS idx_regionalizacion_dane
    ON regionalizacion(codigo_dane);
```

> **Precisión:** `DECIMAL(14,6)` para preservar precisión en departamentos pequeños (Vaupés: ~0.09 mmm en algunos años).
>
> **Totales de región:** Se computan en la API como `SUM()` agrupado por `region`, no se almacenan.

### 3.3 Mapeo de códigos DANE

| Cod. DANE | Departamento | Región |
|---|---|---|
| 05 | Antioquia | ANDINA |
| 08 | Atlántico | CARIBE |
| 11 | Bogotá, D.C. | ANDINA |
| 13 | Bolívar | CARIBE |
| 15 | Boyacá | ANDINA |
| 17 | Caldas | ANDINA |
| 18 | Caquetá | AMAZONIA |
| 19 | Cauca | PACÍFICO |
| 20 | Cesar | CARIBE |
| 23 | Córdoba | CARIBE |
| 25 | Cundinamarca | ANDINA |
| 27 | Chocó | PACÍFICO |
| 41 | Huila | ANDINA |
| 44 | La Guajira | CARIBE |
| 47 | Magdalena | CARIBE |
| 50 | Meta | ORINOQUÍA |
| 52 | Nariño | PACÍFICO |
| 54 | Norte de Santander | ANDINA |
| 63 | Quindío | ANDINA |
| 66 | Risaralda | ANDINA |
| 70 | Sucre | CARIBE |
| 73 | Tolima | ANDINA |
| 76 | Valle del Cauca | PACÍFICO |
| 81 | Arauca | ORINOQUÍA |
| 85 | Casanare | ORINOQUÍA |
| 86 | Putumayo | AMAZONIA |
| 88 | San Andrés y Providencia | INSULAR |
| 91 | Amazonas | AMAZONIA |
| 94 | Guainía | AMAZONIA |
| 95 | Guaviare | AMAZONIA |
| 97 | Vaupés | AMAZONIA |
| 99 | Vichada | ORINOQUÍA |
| — | Por Regionalizar | — |
| — | Nacional | — |

> **Nota sobre nombre de región "AMAZONIA":** El Excel usa `AMAZONAS` (nombre del dpto. también). Se normaliza a `AMAZONIA` en la DB para evitar ambigüedad con el departamento homónimo.

---

## 4. Script ETL propuesto

Archivo: `etl/load_regionalizacion.py`

### Lógica de extracción

```
Para cada hoja del Excel:
  - Detectar años en fila 4 (columnas con valor entero)
  - Detectar offsets de columna por año (Aprop=0, Comp=1, Oblig=2, Pago=3)
  - Iterar filas 6..N:
      si nombre en MAYÚSCULAS → fila de region_total → OMITIR (se recalcula)
      si nombre == 'Total general' → OMITIR
      si nombre == 'Por Regionalizar' → tipo='por_regionalizar', region='POR_REGIONALIZAR'
      si nombre == 'Nacional' → tipo='nacional', region='NACIONAL'
      si nombre en mayúsculas parcial o con tilde → tipo='departamento'
  - Calcular pct_compromisos = comp/aprop*100
  - Calcular pct_participacion = aprop_dpto / total_general_año * 100
  - Convertir millones → miles de millones: valor / 1000
  - Insertar en tabla `regionalizacion`
```

### Mapeo de nombres a código DANE

Se mantiene un diccionario en el script con variantes de nombre (con/sin tilde, abreviaciones):
```python
DANE_MAP = {
    'Amazonas': '91',
    'Caquetá': '18',
    'Bogotá, D.C.': '11',
    'Bogotá D.C.': '11',
    'Archipiélago de San Andrés...': '88',
    # etc.
}
```

---

## 5. Nuevos endpoints API

| Endpoint | Descripción | Parámetros |
|---|---|---|
| `GET /api/regionalizacion` | Totales por región, todos los años | `vigencia` (opcional) |
| `GET /api/regionalizacion/{region}` | Detalle departamentos de una región | `vigencia`, `region` |
| `GET /api/regionalizacion/historico` | Serie histórica 2022-2026 por región | — |
| `GET /api/regionalizacion/departamento/{codigo_dane}` | Serie histórica de un departamento | — |
| `GET /api/regionalizacion/mapa` | Datos para capa GeoJSON (código DANE + pct_compromisos) | `vigencia` |

---

## 6. Cambios al esquema (migración)

### Script de migración: `db/migrations/003_regionalizacion_multiagno.sql`

```sql
-- Crear tabla catálogo DANE
CREATE TABLE IF NOT EXISTS dane_departamentos (...);

-- Crear nueva tabla regionalizacion
CREATE TABLE IF NOT EXISTS regionalizacion (...);

-- Migrar datos existentes de regionalizacion_detalle_2025 (vigencia=2025)
INSERT INTO regionalizacion (bitacora_id, vigencia, tipo, region, departamento,
    apropiacion_mmm, compromisos_mmm, obligaciones_mmm, pagos_mmm, ...)
SELECT bitacora_id, 2025, 'departamento', region, departamento,
    vigente_mm/1000, compromisos_mm/1000, obligaciones_mm/1000, pagos_mm/1000, ...
FROM regionalizacion_detalle_2025;

-- Opcionalmente conservar tablas antiguas (renombrar con prefijo _legacy_)
ALTER TABLE regionalizacion_resumen RENAME TO _legacy_regionalizacion_resumen;
ALTER TABLE regionalizacion_detalle_2025 RENAME TO _legacy_regionalizacion_detalle_2025;
```

---

## 7. Visualizaciones propuestas (posterior a integración)

Una vez cargados los datos, se puede enriquecer la Sección 3 con:

1. **Mapa coroplético multi-año** — actualmente solo muestra 2025; con los datos históricos se puede agregar un slider de año.
2. **Barras apiladas por región** — apropiación histórica 2022-2026 por región (Chart.js, similar a Sec 2).
3. **Tabla dinámica región → departamentos** — expandible por región, con % ejecución por año.
4. **Treemap o donut** — participación % de cada región en el total del año seleccionado.

---

## 8. Decisiones y observaciones

### Decisiones tomadas

| # | Decisión |
|---|---|
| 1 | Nombre de región normalizado: **AMAZONIA** (evita colisión con el departamento homónimo) |
| 2 | Unidad en DB: **miles de millones (mmm)** — consistente con demás tablas; conversión = valor_fuente / 1,000 |
| 3 | Filas "Por Regionalizar" y "Nacional" se cargan como registros especiales (`tipo` diferenciado), no se pintan en el mapa GeoJSON pero sí se incluyen en totales y tablas |

### Observación — Compromisos 2023 inusualmente bajos

En el archivo fuente, los valores de compromisos, obligaciones y pagos para **2023** muestran ejecuciones muy reducidas respecto a los demás años. Ejemplo región ANDINA:

| Año | Aprop. (mmm) | Comp. (mmm) | % Comp/Aprop |
|---|---|---|---|
| 2022 | 26.54 | 13.14 | 49.5% |
| **2023** | **16.29** | **0.48** | **~3%** |
| 2024 | 23.12 | 11.78 | 50.9% |
| 2025 | 22.90 | 7.93 | 34.6% |

Este patrón se repite en todas las regiones para 2023. Puede deberse a que el corte de datos de ese año corresponde a una fecha más temprana del calendario (enero o febrero), distinta al corte de marzo de los demás años. **Se cargan los datos tal como vienen de la fuente** y se agrega una nota descriptiva en los metadatos del endpoint para alertar al consumidor de esta diferencia. Pendiente confirmar fecha de corte real del año 2023 con el equipo DPIP.

### Observación — Sectores principales por departamento

Existe información de inversión desagregada por sector y departamento (apropiación, compromisos, obligaciones, pagos), pero **los datos aún no han sido suministrados**. El campo `principales_sectores` del esquema anterior se elimina de esta versión. Cuando los datos estén disponibles, se diseñará una tabla separada (`regionalizacion_sectores`) y los endpoints correspondientes. Esta integración **no bloquea** el desarrollo actual.
