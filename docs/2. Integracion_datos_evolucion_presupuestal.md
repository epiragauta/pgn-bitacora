# Integración de datos — Sección 2: Evolución Presupuestal PGN

> **Estado al 2026-06-28:** Las secciones 1–5 de este documento describen una propuesta de migración hacia tablas con prefijo `pgn_` (`pgn_concepto`, `pgn_ejecucion`) que **no fue ejecutada**. La implementación actual usa las tablas `evolucion_presupuestal` y `ejecucion_historica` cargadas por `etl/seed_data.py`. La sección 6 documenta el estado real vigente.

---

Este documento describe cómo incorporar el nuevo schema `pgn_concepto` / `pgn_ejecucion`
a la base de datos SQLite de la aplicación sin afectar las demás secciones.

---

## 1. Reemplazar el schema anterior

Las tablas que este módulo reemplaza son `evolucion_presupuestal` y `ejecucion_historica`
(usada parcialmente por la Sección 2). El DDL en `db/schema_pgn.sql` solo opera sobre
tablas con prefijo `pgn_`; el resto del schema (`metadatos_bitacora`, secciones 1, 3–6)
no se toca.

### Pasos recomendados

```bash
# 1. Hacer backup antes de cualquier cambio destructivo
cp db/pgn.db db/pgn.db.bak

# 2. Aplicar solo el DDL del módulo PGN (no el schema.sql completo)
sqlite3 db/pgn.db < db/schema_pgn.sql

# 3. Importar el CSV con datos 2022-2026
python etl/importar_pgn.py data/evolucion_presupuestal.csv db/pgn.db

# 4. Verificar
sqlite3 db/pgn.db "SELECT COUNT(*) FROM pgn_concepto; SELECT COUNT(*) FROM pgn_ejecucion;"
```

Si necesitas mantener la base de datos de producción intacta mientras preparas la migración,
usa ATTACH para trabajar sobre una copia:

```sql
-- Desde una sesión SQLite apuntando a la BD de producción
ATTACH 'db/pgn_nuevo.db' AS nuevo;
-- ...importar en pgn_nuevo.db, validar, luego renombrar el archivo
```

### ¿Qué pasa con las tablas antiguas?

`evolucion_presupuestal` y `ejecucion_historica` no se eliminan automáticamente.
Si la API todavía referencia esas tablas, actualiza los endpoints correspondientes
antes de eliminarlas manualmente:

```sql
DROP TABLE IF EXISTS evolucion_presupuestal;
-- ejecucion_historica también es usada por Sección 4; evaluar antes de borrar
```

---

## 2. Actualizar datos año a año

El flujo para incorporar un nuevo año (ej. 2027) es siempre el mismo:

1. Agregar las filas del nuevo año al CSV (mismas columnas, mismos conceptos o nuevos).
2. Ejecutar el script — usa `INSERT OR REPLACE` con la restricción `UNIQUE(anio, fase, concepto_id)`,
   por lo que registros existentes se sobreescriben y los nuevos se insertan sin duplicar.

```bash
python etl/importar_pgn.py data/evolucion_presupuestal_2027.csv db/pgn.db
```

Si el CSV acumula todos los años (como el actual), re-ejecutar el script es seguro:
el DDL borra y recrea las tablas, luego los 560+ registros se insertan frescos.

Para una actualización incremental sin DROP (conservar histórico mientras se añade un año):

```python
# Alternativa: llamar solo a los pasos C sin ejecutar el DDL
# Requiere que los conceptos ya existan en pgn_concepto.
conn.executemany(
    'INSERT OR REPLACE INTO pgn_ejecucion(anio, fase, concepto_id, valor) VALUES(?,?,?,?)',
    hechos_nuevos
)
```

### Agregar 2027 a la vista crosstab

La vista `pgn_vista_crosstab` tiene los años hardcodeados. Al incorporar un nuevo año,
agregar el bloque correspondiente en `db/schema_pgn.sql` y re-ejecutar el DDL:

```sql
MAX(CASE WHEN e.anio=2027 AND e.fase='Vigente'       THEN e.valor END) AS vigente_2027,
MAX(CASE WHEN e.anio=2027 AND e.fase='Comprometido'  THEN e.valor END) AS comprometido_2027,
MAX(CASE WHEN e.anio=2027 AND e.fase='Obligado'      THEN e.valor END) AS obligado_2027,
MAX(CASE WHEN e.anio=2027 AND e.fase='Pagado'        THEN e.valor END) AS pagado_2027,
```

---

## 3. Convención de nombres

Todas las tablas y vistas de este módulo usan el prefijo **`pgn_`**:

| Objeto             | Tipo  | Propósito                                      |
|--------------------|-------|------------------------------------------------|
| `pgn_concepto`     | Tabla | Jerarquía de conceptos presupuestales          |
| `pgn_ejecucion`    | Tabla | Hechos: valor por (año, fase, concepto)        |
| `pgn_vista_crosstab` | Vista | Formato ancho para compatibilidad con la app |

Esta convención garantiza que nunca colisione con las tablas de otras secciones
(`inversion_*`, `ejecucion_*`, `regionalizacion_*`, `vigencias_*`).

Si en el futuro se modulariza la BD, el prefijo permite identificar y migrar
el módulo de forma autónoma.

---

## 4. Conectar las queries a la API FastAPI

Los cinco queries de `db/queries_evolucion.sql` se traducen directamente a endpoints
en `api/main.py`. Ejemplo para Q1:

```python
@app.get("/api/evolucion/total_pgn")
def evolucion_total_pgn(db=Depends(get_db)):
    rows = db.execute("""
        SELECT e.anio, e.fase, e.valor
        FROM   pgn_ejecucion e
        JOIN   pgn_concepto  c ON c.id = e.concepto_id
        WHERE  c.nombre = 'Total PGN'
          AND  c.unidad = 'Miles mm COP'
        ORDER BY e.anio, e.fase
    """).fetchall()
    return rows_to_list(rows)
```

Para Q2 y Q4 (con parámetros), usar Query params de FastAPI:

```python
@app.get("/api/evolucion/composicion")
def composicion_vigente(anio: int, db=Depends(get_db)):
    rows = db.execute("... WHERE e.anio = ?", (anio,)).fetchall()
    return rows_to_list(rows)
```

### Endpoint de drilldown jerárquico (Q4)

```python
@app.get("/api/evolucion/drilldown")
def drilldown(concepto: str, anio: int, fase: str = "Vigente", db=Depends(get_db)):
    rows = db.execute("""
        WITH RECURSIVE arbol AS (
            SELECT id, nombre, nivel, padre_id, orden
            FROM   pgn_concepto WHERE nombre = ?
            UNION ALL
            SELECT c.id, c.nombre, c.nivel, c.padre_id, c.orden
            FROM   pgn_concepto c JOIN arbol a ON c.padre_id = a.id
        )
        SELECT a.nivel, a.nombre, e.valor, COALESCE(p.nombre,'') AS padre
        FROM   arbol a
        JOIN   pgn_ejecucion  e ON e.concepto_id = a.id
        LEFT JOIN pgn_concepto p ON p.id = a.padre_id
        WHERE  e.anio = ? AND e.fase = ?
        ORDER BY a.orden
    """, (concepto, anio, fase)).fetchall()
    return rows_to_list(rows)
```

---

## 5. Datos de fallback en el frontend

El frontend (`frontend/index.html`) tiene un objeto `D` con datos embebidos como fallback
cuando la API no responde. Si se actualizan los datos en la BD, actualizar también el
objeto `D.evolucion` con los valores del Q1 para que el modo offline muestre cifras recientes.

Fecha: 27 de junio de 2026

---

## 6. Estado real de implementación (2026-06-28)

### 6.1 Tablas activas en la base de datos

La Sección 2 usa las siguientes tablas, creadas por `etl/seed_data.py` y definidas en `db/schema.sql`:

| Tabla | Descripción | Comparte con |
|---|---|---|
| `evolucion_presupuestal` | PGN 2022-2025: Funcionamiento, Inversión, Servicio Deuda, Total | Solo Sec 2 |
| `ejecucion_historica` | % Compromisos, % Obligaciones, % Pagos y % PIB por vigencia | Sec 2 y Sec 4 |

No existen las tablas `pgn_concepto`, `pgn_ejecucion` ni la vista `pgn_vista_crosstab`. Los scripts `importar_pgn.py` y `schema_pgn.sql` referenciados en las secciones anteriores tampoco fueron creados.

### 6.2 Endpoints API vigentes para Sec 2

| Endpoint | Descripción |
|---|---|
| `GET /api/evolucion` | Datos PGN 2022-2025: funcionamiento, inversión, deuda, total (mmm) |
| `GET /api/evolucion/inversion_historica` | % compromisos, % obligaciones, % pagos, % PIB por vigencia |

### 6.3 Datos cargados (seed_data.py)

**`evolucion_presupuestal`** — 4 vigencias × 4 conceptos = 16 registros:

| Vigencia | Funcionamiento | Inversión | Serv. Deuda | Total PGN |
|---|---|---|---|---|
| 2022 | 210,990 | 69,626 | 71,665 | 352,281 |
| 2023 | 253,434 | 74,222 | 77,998 | 405,655 |
| 2024 | 308,251 | 99,851 | 94,522 | 502,624 |
| 2025 | 329,237 | 83,961 | 112,605 | 525,803 |

**`ejecucion_historica`** — 4 vigencias con % ejecución al corte de marzo:

| Vigencia | % Comp | % Oblig | % Pagos | % PIB |
|---|---|---|---|---|
| 2022 | 53 | 16 | 16.0 | 4.7 |
| 2023 | 43 | 11 | 11.3 | 5.3 |
| 2024 | 39 | 9 | 8.7 | 5.3 |
| 2025 | 43 | 8 | 7.8 | 4.6 |

### 6.4 Visualizaciones frontend implementadas

- **Gráfico de barras apiladas**: evolución anual del PGN 2022-2025 por componente (funcionamiento / inversión / servicio deuda).
- **Gráfico de líneas dual**: inversión como % del PIB y % del gasto total, serie 2022-2025.
- **Tabla comparativa**: resumen 4 vigencias con botón de expandir/colapsar.
- **Cards de KPI**: datos clave del periodo vigente (apropiación, compromisos, obligaciones, pagos).

### 6.5 Próximos pasos para una migración real a pgn_*

Si en el futuro se decide migrar al esquema propuesto en secciones 1–5:
1. Crear `db/schema_pgn.sql` con el DDL de las tres tablas.
2. Crear `etl/importar_pgn.py` que lea el CSV de evolución presupuestal.
3. Actualizar los endpoints `/api/evolucion` y `/api/evolucion/inversion_historica` para consultar las nuevas tablas.
4. Actualizar el objeto `D.evolucion` en `frontend/index.html` como fallback.
5. Conservar o renombrar las tablas anteriores hasta validar la migración.

---

## 7. Fuente primaria identificada — archivo Excel 2026-I

**Fecha de identificación:** 2026-06-29

### 7.1 Archivo fuente

```
C:\ws\dnp\ws\BASES_BITACORA\2026\Marzo\2. EVOLUCIÓN PRESUPUESTAL\
  └── 2026-03-31Estructura_Evolución PGN y Reg-Ejec-marzo.xlsx
        └── Evolucion PGN   ← hoja relevante (única con datos PGN)
```

**Otras hojas del libro:** `Rezago sectores`, `Rezago hist.`, `Rezago Graf. 2`, `Pérdida Aprop. Graf. 2`

### 7.2 Estructura de la hoja `Evolucion PGN`

| Elemento | Valor |
|---|---|
| Título (fila 1) | "Evolución del PGN por tipo de gasto Acumulada a Marzo 2022–2023–2024–2025–2026" |
| Unidades declaradas (fila 2) | **Miles de millones de pesos** (ya en mmm — no requiere conversión) |
| Estructura de columnas | 5 años × 4 fases = 20 columnas de datos |
| Fases por año | Vigente (col 1), Comp. (col 2), Obl. (col 3), Pago (col 4) |
| Fila de encabezado de años | Fila 5 (celdas combinadas: 2022, 2023, 2024, 2025, 2026) |
| Fila de sub-encabezado de fases | Fila 9 |
| Filas de datos | 6–35 (28 filas, contando porcentajes y el duplicado) |

### 7.3 Jerarquía de conceptos (filas 6–35)

La tabla tiene una estructura jerárquica de 3 niveles. Las filas de porcentaje (`% PIB`) intercalan con las filas de valores absolutos:

| Fila | Concepto | Nivel | Tipo |
|---|---|---|---|
| 6 | PGN como % PIB | — | % (calculado) |
| 7 | **Total PGN** | Nivel 1 | Valor |
| 8 | Funcionamiento como % PIB | — | % (calculado) |
| 9 | *(sub-encabezado fases)* | — | Header |
| 10 | **Funcionamiento** | Nivel 1 | Valor |
| 11 | Gastos de personal | Nivel 2 | Valor |
| 12 | Adquisición de Bienes y Servicios | Nivel 2 | Valor |
| 13 | Transferencias | Nivel 2 | Valor |
| 14 | → SGP | Nivel 3 | Valor |
| 15 | → Pensiones | Nivel 3 | Valor |
| 16 | → Instituciones de Educación Superior Pública sin Pensiones | Nivel 3 | Valor |
| 17 | → Resto | Nivel 3 | Valor |
| 18 | Gastos de Comercialización y Producción | Nivel 2 | Valor |
| 19 | Adquisición de Activos Financieros | Nivel 2 | Valor |
| 20 | Disminución de Pasivos | Nivel 2 | Valor |
| 21 | Gastos por Tributos, Multas, Sanciones e Intereses de Mora | Nivel 2 | Valor |
| 22 | Servicio de la Deuda como % PIB | — | % (calculado) |
| 23 | **Servicio de la Deuda** | Nivel 1 | Valor |
| 24 | Servicio de la Deuda Pública Externa | Nivel 2 | Valor |
| 25 | → Principal | Nivel 3 | Valor |
| 26 | → Intereses | Nivel 3 | Valor |
| 27 | → Comisiones y Otros Gastos | Nivel 3 | Valor |
| 28 | Servicio de la Deuda Pública Interna *(typo: "Serivicio")* | Nivel 2 | Valor |
| 29 | → Principal | Nivel 3 | Valor |
| 30 | → Intereses | Nivel 3 | Valor |
| 31 | → Comisiones y Otros Gastos | Nivel 3 | Valor |
| 32 | Fondo de contingencias | Nivel 2 | Valor |
| 33 | Inversión como % PIB | — | % (calculado) |
| 34 | **Inversión** | Nivel 1 | Valor |
| 35 | Inversión *(fila duplicada — valores idénticos a F34)* | — | Duplicado |

### 7.4 Datos disponibles en el Excel vs datos actualmente en la BD

| Vigencia | Excel Func. Vigente | seed_data Func. | Excel Inv. Vigente | seed_data Inv. | Excel Deuda Vigente | seed_data Deuda | Excel Total Vigente | seed_data Total |
|---|---|---|---|---|---|---|---|---|
| 2022 | 208,808 | 210,990 | 69,626 | 69,626 | 71,665 | 71,665 | 350,098 | 352,281 |
| 2023 | 258,069 | 253,434 | 74,222 | 74,222 | 77,998 | 77,998 | 410,290 | 405,655 |
| 2024 | 289,892 | 308,251 | 99,851 | 99,851 | 94,522 | 94,522 | 484,266 | 502,624 |
| 2025 | 319,191 | 329,237 | 83,961 | 83,961 | 112,605 | 112,605 | 515,757 | 525,803 |
| **2026** | **327,314** | — | **88,401** | — | **100,450** | — | **516,165** | — |

> Los valores de **Inversión y Servicio de la Deuda coinciden exactamente** entre el Excel y el seed. Las diferencias en **Funcionamiento y Total** se explican porque el seed_data fue generado a partir de la Bitácora 2025-I (corte marzo 2025), con revisiones históricas distintas a las del archivo de la Bitácora 2026-I.

> El **año 2026** completo está disponible en el Excel pero **no está cargado en la BD** (seed_data solo cubre 2022–2025).

### 7.5 Problemas de calidad de datos en la fuente

| Tipo | Descripción | Impacto |
|---|---|---|
| **Fila duplicada** | F34 y F35 son idénticas ("Inversión", mismos valores) | Un ETL debe ignorar una de las dos; si se itera sobre todas las filas sin filtrar, se duplicará el valor |
| **Error tipográfico** | F28: "Serivicio de la Deuda Pública Interna" (falta una "v") | Sin impacto funcional, pero el nombre no coincide con la denominación oficial |
| **Porcentajes intercalados** | Las filas de `% PIB` (F6, F8, F22, F33) están entre filas de valores | Un ETL debe distinguir las filas de porcentaje de las de valor absoluto |

### 7.6 Lo que la BD actual no aprovecha de esta fuente

La BD almacena únicamente los 4 rubros de nivel 1 (Total PGN, Funcionamiento, Inversión, Servicio de la Deuda). El Excel ofrece:
- 3 niveles jerárquicos con 24 conceptos adicionales de detalle
- Los datos del año 2026 (no cargados)
- Las 4 fases de ejecución (Vigente, Compromisos, Obligaciones, Pagos) para cada concepto y año

### 7.7 Recomendación ETL

Crear `etl/load_evolucion_presupuestal.py` que lea esta hoja y cargue:
1. Los 4 rubros de nivel 1 en `evolucion_presupuestal` (reemplazando los datos hardcodeados del seed)
2. Opcionalmente: la jerarquía completa en tablas `pgn_concepto` / `pgn_ejecucion` (esquema propuesto en secciones 1–5)

La lectura es directa porque las unidades ya están en mmm. El único pre-procesamiento necesario es:
- Ignorar la fila duplicada F35
- Omitir las filas de porcentaje (F6, F8, F22, F33) y el sub-encabezado (F9)
- Usar la columna 1 (índice 1) como "Vigente" para cada año