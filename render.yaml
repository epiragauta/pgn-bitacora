services:
  - type: web
    name: pgn-bitacora
    runtime: python
    buildCommand: pip install -r requirements.txt && python etl/seed_data.py
    startCommand: uvicorn api.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
